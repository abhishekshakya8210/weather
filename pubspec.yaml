name: weather
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.5.0
  lottie: ^3.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # 👇 Added for app icons
  flutter_launcher_icons: ^0.14.4

# 👇 Launcher icon settings (all devices)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/image/logo.png"   # apna logo yaha rakho
  remove_alpha_ios: true

flutter:
  uses-material-design: true

  assets:
    - assets/image/
    - assets/sunny.json
    - assets/rain.json
    - assets/cloudy.json
    - assets/snowfall.json
